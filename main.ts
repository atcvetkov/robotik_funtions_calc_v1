/**
 * math_operation
 * 
 * plus  -> +
 * 
 * minus -> -
 * 
 * mult  -> *
 * 
 * div   -> /
 */
function calc (math_operation: string, var_1: number, var_2: number) {
	
}
basic.forever(function () {
	
})
